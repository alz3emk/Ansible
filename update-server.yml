---
- name: Gather System Facts without Root Access
  hosts: your_target_host
  gather_facts: true
  become: yes
  become_user: your_non_root_user

  tasks:
    - name: Display System Facts
      debug:
        var: ansible_facts
